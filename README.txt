***********************
To run the application it's enough just to call "mvnw spring-boot:run" from root directory.

Default DB url is 'D:/db/loopme', you can set custom value in application.properties#spring.datasource.url